# Default values for smarter-cloud.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: smarter

smarter-dns:
  configuration:
    nodeSelector:
      smarter-edge: enabled

smarter-cni:
  configuration:
    nodeSelector:
      smarter-edge: enabled

smarter-device-manager:
  configuration:
    nodeSelector:
      smarter-edge: enabled

